import React from 'react';
import Layout from 'components/Layout';
import Header from 'components/Header';

const Blog = () => {
  return (
    <Layout>
      <Header />
    </Layout>
  );
};

export default Blog;
