import styled from 'styled-components';

export const Container = styled.div`
  background: #fff;
  font-size: 1.6rem;
  line-height: 1.6;

  input,
  textarea,
  button,
  select {
    font-size: 1.6rem;
    line-height: 1;
  }

  textarea {
    line-height: 1.4;
  }
`;
